import numpy as np 
import math, random, copy

with open(test.data,w) as piece:

	piece.write("Hello World")
	piece.write("This is our new text file")
	piece.write("and this is another line.")
	piece.write("Why? Because we can.")

	piece.close()

